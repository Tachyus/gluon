os: Visual Studio 2015
version: '{build}'
branches:
  except:
    - gh-pages
clone_depth: 10
build:
  verbosity: minimal
build_script:
  - ps: .\build.ps1 BuildPackage
test: off
artifacts:
  - path: 'bin\*.nupkg'
    name: Gluon
